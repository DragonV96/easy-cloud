package ${packageName}.${moduleName}.${dtoPackageName}.${requestPackageName};

#if($swaggerSwitch)
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
#end
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.ToString;

import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import java.io.Serializable;


/**
* @author : ${author}
* @date : ${date}
* @time : ${time}
* @Description : ${comment}新增请求对象
*/
@EqualsAndHashCode
@Data
@ToString
@ApiModel(value = "${comment}新增请求对象")
public class Add${entityName}${requestSuffix} implements Serializable {

    private static final long serialVersionUID = 1L;

#foreach(${column} in ${columnMap})
#end
}